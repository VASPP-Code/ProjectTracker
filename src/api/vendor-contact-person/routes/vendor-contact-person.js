'use strict';

/**
 * vendor-contact-person router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::vendor-contact-person.vendor-contact-person');
