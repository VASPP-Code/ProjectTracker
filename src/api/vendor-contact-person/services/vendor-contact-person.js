'use strict';

/**
 * vendor-contact-person service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vendor-contact-person.vendor-contact-person');
