'use strict';

/**
 *  customer-contact-person controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::customer-contact-person.customer-contact-person');
