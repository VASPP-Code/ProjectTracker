'use strict';

/**
 * customer-contact-person service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::customer-contact-person.customer-contact-person');
