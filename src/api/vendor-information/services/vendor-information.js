'use strict';

/**
 * vendor-information service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vendor-information.vendor-information');
