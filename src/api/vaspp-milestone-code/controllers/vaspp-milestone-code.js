'use strict';

/**
 *  vaspp-milestone-code controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::vaspp-milestone-code.vaspp-milestone-code');
