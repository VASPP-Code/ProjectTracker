'use strict';

/**
 * vaspp-milestone-code service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vaspp-milestone-code.vaspp-milestone-code');
