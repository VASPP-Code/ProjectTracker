'use strict';

/**
 * project-milestone service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::project-milestone.project-milestone');
